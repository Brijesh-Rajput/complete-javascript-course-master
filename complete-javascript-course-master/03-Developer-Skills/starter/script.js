// Remember, we're gonna use strict mode in all scripts now!
"use strict";

// Prettier --> extension. Converts every single quotes into double quotes.

/*
node -v  //to see the version of nodejs
npm install live-server -g //g represent that globally live-server tool will install by npm library
live-server //to start live-server.
*/